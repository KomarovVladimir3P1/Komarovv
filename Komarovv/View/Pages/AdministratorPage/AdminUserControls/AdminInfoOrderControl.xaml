<UserControl x:Class="Komarovv.View.Pages.AdministratorPage.AdminUserControls.AdminInfoOrderControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Komarovv.View.Pages.AdministratorPage.AdminUserControls" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             d:DesignHeight="500"
    d:DesignWidth="1000"
    mc:Ignorable="d">
    <Grid>
        <StackPanel
            Margin="5"
            HorizontalAlignment="Center"
            VerticalAlignment="Center">
            <DataGrid
                x:Name="DataOrderInfo"
                Width="950"
                Height="450"
                AlternatingRowBackground="#F0fff0"
                AutoGenerateColumns="False"
                Background="#D4A84F"
                CanUserAddRows="False"
                CanUserDeleteRows="False"
                CanUserResizeColumns="False"
                CanUserResizeRows="False"
                CanUserSortColumns="False"
                ColumnHeaderHeight="50"
                Effect="{StaticResource MaterialDesignElevationShadow16}"
                FontSize="15"
                GridLinesVisibility="Horizontal"
                IsReadOnly="True"
                RowBackground="White">
                <DataGrid.Columns>
                    <DataGridTextColumn
                        Width="Auto"
                        Binding="{Binding OrderTitle}"
                        FontSize="15"
                        Header="ЗАКАЗ" />
                    <DataGridTextColumn
                        Width="Auto"
                        Binding="{Binding Employer.CompanyName}"
                        FontSize="15"
                        Header="НАЗВАНИЕ КОМПАНИИ" />
                    <DataGridTextColumn
                        Width="Auto"
                        Binding="{Binding User.FIO}"
                        FontSize="15"
                        Header="ФРИЛАНСЕР" />
                    <DataGridTextColumn
                        Width="Auto"
                        Binding="{Binding OrderDeadline}"
                        FontSize="15"
                        Header="СРОКИ" />
                    <DataGridTemplateColumn>
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button
                                    x:Name="BtnDelete"
                                    Background="#D4A84F"
                                    BorderBrush="#D4A84F"
                                    Click="BtnDelete_Click"
                                    Foreground="#4D3825"
                                    Style="{StaticResource MaterialDesignFlatLightButton}"
                                    ToolTip="Удалить компанию">
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="Remove" />
                                    </StackPanel>
                                </Button>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Grid>
</UserControl>
